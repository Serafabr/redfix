export type SizeType = {
  width: number,
  height: number,
  strokeWidth: number,
}

export type ColorType = {
  fillColor: string,
  strokeColor: string,
}

export enum PriorityType { Low, Normal, High };